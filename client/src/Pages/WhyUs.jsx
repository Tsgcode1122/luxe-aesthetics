import React, { useEffect } from "react";

const WhyUs = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  });
  return (
    <div>
      <QuestionnaireSubmit />
    </div>
  );
};

export default WhyUs;
